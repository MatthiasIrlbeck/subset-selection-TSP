// Generated by scripts/generate_options.py from config/options.json. DO NOT EDIT.

    // External oracle: none, auto, lkh, or concorde.
    ExternalOracleMode mode = ExternalOracleMode::None;
    // Oracle problem format: matrix or euc2d.
    OracleProblemFormat problem_format = OracleProblemFormat::Matrix;
    // LKH executable path.
    std::string lkh_path = "LKH";
    // Concorde executable path.
    std::string concorde_path = "concorde";
    // Per-call oracle time limit; zero disables it.
    int time_limit_sec = 0;
    // Distance scaling factor for integer oracle matrices.
    int scale = 1000000;
    // Top full-TSP solutions sent to the oracle.
    int tsp_top = 1;
    // Top subset solutions sent to the oracle.
    int subset_top = 1;
    // Minimum cardinality eligible for an external oracle.
    int min_k = kExactSmallTourLimit + 1;
    // Maximum cardinality eligible for an external oracle.
    int max_k = 2500;
    // LKH RUNS parameter.
    int lkh_runs = 6;
    // LKH MAX_TRIALS; zero uses the solver default.
    int lkh_max_trials = 0;
    // Disable external-oracle use for full TSP.
    bool use_for_tsp = true;
    // Disable external-oracle use for subset tours.
    bool use_for_subset = true;
    // Feed oracle-improved cycles back into the in-process elite search.
    bool inline_feedback = false;
    // Print external-oracle diagnostics.
    bool verbose = false;
