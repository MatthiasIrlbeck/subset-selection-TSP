// Generated by scripts/generate_options.py from config/options.json. DO NOT EDIT.

    // Number of random points.
    int N = 500;
    // Monte Carlo point-set instances.
    int instances = 15;
    // Instance worker threads; zero selects an automatic bounded value.
    int threads = 0;
    // Conservative peak-memory budget in MiB; zero derives a safe automatic budget from
    // currently available physical/container memory.
    int memory_budget_mb = 0;
    // Use flat-torus periodic boundary conditions.
    bool periodic = false;
    // Compute the two-nearest-neighbor control variate and fixed-subset bound; the bound
    // measures tour quality conditional on that subset.
    bool control_variate = false;
    // Cheap KNN-only samples used to estimate the full-set control-variate expectation.
    int cv_mc_samples = 2000;
    // Compute the Held-Karp lower bound for each selected subset; this certifies tour
    // quality conditional on that subset, not global subset optimality.
    bool held_karp = false;
    // Held-Karp subgradient iterations.
    int hk_iterations = 400;
    // Print per-instance and per-p progress.
    bool verbose = false;
    // Include per-instance rows and restart diagnostics in JSON.
    bool include_instance_rows = false;
    // Stable campaign identity.
    std::string campaign_id = "default";
    // Nonnegative campaign shard identity.
    int campaign_shard = 0;
    // Campaign-global first replicate identifier.
    int replicate_offset = 0;
    // Point-instance seed; defaults to --seed.
    std::optional<int> point_seed = std::nullopt;
    // Heuristic-search seed; defaults to --seed.
    std::optional<int> search_seed = std::nullopt;
    // Stable solver-policy identity.
    std::string solver_policy_id = "default";
    // Fidelity identity such as cheap or strong.
    std::string fidelity_level = "strong";
    // Run an ascending continuation sweep after the descending sweep.
    bool second_sweep = false;
    // Replace an existing output path.
    bool force_output = false;
    // Validate and print the resolved configuration without solving.
    bool dry_run = false;
    // Print the resolved configuration before solving.
    bool dump_config = false;
    // Output JSON path.
    std::string output_path = "results.json";
    // Output durability: none, file, or full (file plus parent directory).
    OutputDurability output_durability = OutputDurability::Full;
    // Resolved probability grid.
    std::vector<double> p_values = {0.02, 0.03, 0.05, 0.07, 0.1, 0.15, 0.2, 0.25, 0.3, 0.4, 0.5, 0.6, 0.8, 1.0};
